kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

networking:
  disableDefaultCNI: false

nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30080   # NodePort access
    hostPort: 8080
  - containerPort: 30443
    hostPort: 8443

- role: worker
- role: worker