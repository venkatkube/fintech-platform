apiVersion: v1
kind: Service
metadata:
  name: txn-service
  labels:
    app: txn-service
    tier: backend
spec:
  type: ClusterIP

  selector:
    app: txn-service

  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP