apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fintech-services-prod
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/venkatkube/fintech-platform
    targetRevision: HEAD
    path: k8s/overlays/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: fintech-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true